[package]
name = "vestroll_vault"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true }
vestroll_common = { path = "../common" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["alloc", "testutils"] }